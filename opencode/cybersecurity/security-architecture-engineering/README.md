# OpenCode Security Architecture Engineering

Native OpenCode package for Cybersecurity Area 02. It uses project instructions, `opencode.jsonc`, Markdown agents, and Skills to support advisory security architecture work.

No active MCP server, plugin, hook, shell execution, deployment, live configuration, or production control operation is enabled.

