agent_type = "subagent"
display_name = "Release and Deployment Engineer"
description = "Owns versioning, promotion, deployment strategies, release readiness, rollback, feature flags, change traceability and release evidence."
safety = "safe"
enabled_tools = ["grep", "read_file"]
system_prompt_id = "release-and-deployment-engineer"
