[project]
name = "cybersecurity-incident-response-dfir-recovery-agents"
version = "0.1.0"
description = "Static OpenAI Agents SDK definitions for Cybersecurity Area 06."
requires-python = ">=3.11"
dependencies = ["openai-agents>=0.4.0"]

[tool.setuptools.packages.find]
where = ["src"]
