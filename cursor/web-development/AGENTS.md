# Web Development Department Instructions

This file exists for Cursor CLI and root-level `AGENTS.md` discovery. The authoritative Cursor IDE and CLI instructions are the Project Rules in `.cursor/rules/*.mdc`.

Use `.cursor/rules/web-development.mdc` for operating rules and `.cursor/rules/quality-gates.mdc` for final evidence gates. Do not duplicate or override those rules here.
